function main(input) {
    const args = input.split("\n");

    const [numberOfPassengers, capacityOfBus, timeLimit] = args[0].split(' ');
  }
  
  main(require("fs").readFileSync(process.stdin.fd, "utf8"));
  